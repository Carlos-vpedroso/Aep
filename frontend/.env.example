# URL base da API
NEXT_PUBLIC_API_URL=sua_url_api
NEXT_PUBLIC_SOCKET_URL=sua_url_api
# Chave secreta para assinar e validar JWTs
# IMPORTANTE: mantenha essa chave segura e não compartilhe publicamente
# Gere a chave utilizando o código abaixo
JWT_SECRET=your_jwt_secret_here
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
